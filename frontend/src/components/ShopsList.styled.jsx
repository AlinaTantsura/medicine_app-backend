import styled from "styled-components";

export const ShopListStyled = styled.ul({
    listStyle: 'none',
    padding: "0", 
    display: "flex",
    flexDirection: "column",
    gap: "10px",
    alignItems: "center"
})